import db from './src/config/database'; db('manufacturing_sales_results').join('companies', 'companies.id', 'manufacturing_sales_results.company_id').where('world_orbit', 1).orderBy('world_arc').then((r: any[]) => { console.table(r.map((x: any) => ({arc: x.world_arc, company: x.name, share: x.market_share_estimate, reason: x.main_reason_code, sales: x.units_sold}))); process.exit() });
