const fs = require('fs'); const data = JSON.parse(fs.readFileSync('scripts/replacements2.json', 'utf8')); let txt = fs.readFileSync('src/api/controllers/manufacturing.controller.ts', 'utf8'); for (const r of data) { if (txt.includes(r.find)) { txt = txt.replace(r.find, r.replace); console.log('Replaced successfully: ' + r.find.substring(0, 30)); } else { console.log('Failed to find: ' + r.find.substring(0, 30)); } } fs.writeFileSync('src/api/controllers/manufacturing.controller.ts', txt);
