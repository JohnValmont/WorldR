[{"find": "const factories = await trx(\u0027manufacturing_factories\u0027).where({ company_id: companyId, status: \u0027active\u0027 });", "replace": "console.log(`[produceForCompany] Arc ${clock.current_arc} Fetching factories for ${companyId}...`); const factories = await trx(\u0027manufacturing_factories\u0027).where({ company_id: companyId, status: \u0027active\u0027 }); console.log(`[produceForCompany] Arc ${clock.current_arc} Fetched factories for ${companyId}`);"}, {"find": "const productionLines = await trx(\u0027manufacturing_production_lines\u0027)", "replace": "console.log(`[produceForCompany] Arc ${clock.current_arc} Fetching productionLines for ${companyId}...`); const productionLines = await trx(\u0027manufacturing_production_lines\u0027)"}, {"find": "const staff = await trx(\u0027company_staff\u0027).where({ company_id: companyId });", "replace": "console.log(`[produceForCompany] Arc ${clock.current_arc} Fetched productionLines for ${companyId}`); const staff = await trx(\u0027company_staff\u0027).where({ company_id: companyId });"}]
