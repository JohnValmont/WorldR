const sqlite3 = require('sqlite3'); const db = new sqlite3.Database('worldr.sqlite'); db.all('SELECT sql FROM sqlite_master WHERE type=''table''', (err, rows) => { console.log(rows.find(r => r.sql && r.sql.includes('pol_campaign_actions'))); });
